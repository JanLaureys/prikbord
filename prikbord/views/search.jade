extends layout

block content
  .row
    .col-lg-12
      if search_query
        h1.page-header Zoeken naar "#{search_query}"
      else
        h1.page-header Alle berichten
  .row
    .col-lg-12.page-messages
      each message, i in messages
        +message(message)
